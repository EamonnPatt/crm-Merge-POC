services:
  - type: web
    name: crm-merge-poc
    runtime: node
    plan: free
    rootDir: web-app
    # --include=dev: the build needs Vite/TypeScript even if NODE_ENV=production is set.
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /healthz
