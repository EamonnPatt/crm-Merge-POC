{
  "name": "add-impact-sales-platform",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "build": "npm --prefix web-app ci --include=dev && npm --prefix web-app run build",
    "start": "node server.js"
  },
  "dependencies": {
    "compression": "^1.8.2",
    "express": "^5.2.1"
  }
}
